import { SpoilerDirective } from './spoiler.directive';

describe('SpoilerDirective', () => {
  it('should create an instance', () => {
    const directive = new SpoilerDirective();
    expect(directive).toBeTruthy();
  });
});
